#include <stdio.h>
int main(void)
{
    int n,i;
    printf("Enter the value of n:");
    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        printf("%d/%d ",1,i);
        if(i<n)
            printf("+ ");
    }
    return(0);
}
